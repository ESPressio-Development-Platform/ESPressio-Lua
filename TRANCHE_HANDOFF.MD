# ESPressio-Lua implementation tranche

## Scope
Implement Lua 5.5.1 embedding entirely in ESPressio-Lua on main. Provide reusable native-type Lua definitions; explicitly exposed constructors, fields, methods and properties; per-instance type registration; Lua-owned and borrowed native objects; checked conversion; functions and constants; execution/error and memory contracts; tests, demos and documentation. Halt and notify the user if another repository requires modification. No other repository or branch may be modified.

## Rules
Follow Policy/ABSTRACTION_POLICY.MD, CIRCULAR_DEPENDENCY_POLICY.MD, CROSS_LIBRARY_CHANGES.MD (user's halt instruction takes precedence), DEMO_POLICY.MD, DOCUMENTATION_POLICY.MD, GITHUB_TASK_RESULT_POLICY.MD, LOGGING_INSTRUMENTATION_POLICY.MD, OBSERVER_CALLBACK_POLICY.MD, TRANCHE_TIMEOUT_OR_INTERRUPTION_POLICY.MD, WORKING_BRANCHES.MD (user explicitly selects Lua main). Reuse System allocation abstractions. No platform-specific code in the binding. No circular dependencies. Demos must compile and include Arduino IDE and PlatformIO variants. Document public symbols. Preserve upstream Lua license. Tests must exercise actual Lua 5.5.1. Update this log throughout work; deliver completed handoff then remove it from the repository.

## UTC activity
- 2026-09-05T13:37:00Z: Inspected main: only LICENSE and .gitignore exist. Read all ten organization policies. GitHub connector access works; direct git clone lacks authentication. No external repository changes identified or made. Beginning dependency API inspection and local implementation.

- 2026-09-05T13:52:00Z: Confirmed existing System/structural_realignment allocator requires no modification. Acquired official Lua v5.5.1 tag 7579fc9d7ed90240487251dfb69168f8e64e9294; source is unmodified, compiled as C++ for RAII-safe Lua errors. Implemented reusable type definitions, constructors, fields/properties, methods, borrowed objects, per-instance registrations, checked conversions, modules, functions/constants, protected execution and resource accounting locally.
- 2026-09-05T13:53:00Z: Native smoke example and ASan/UBSan runtime tests pass. Tests cover lifetime, independent views/VMs, invalid inputs, native exceptions, reentrancy/concurrency rejection, instruction limits, alignment, initialization and registration allocation-failure sweeps. LeakSanitizer cannot inspect this container's process namespace; it is disabled locally, with explicit provider allocation/deallocation checks retained. Initial log timestamp corrected to the executor UTC clock. Preparing build packaging, demos and documentation; no external repository modified.

- 2026-09-05T14:12:00Z: Finalizer testing exposed Lua's deliberate hook suppression during GC finalizers. Changed the exposed setmetatable operation to reject Lua-defined __gc handlers; native noexcept finalizers remain enabled. Updated tests/documentation. ESP32 compiler required <climits> before Lua headers; fixed in the binding wrapper without editing upstream Lua.
- 2026-09-05T14:16:00Z: Final native ASan/UBSan tests passed (2/2). Both Arduino sketch and PlatformIO main compiled/linked with Arduino ESP32 2.0.17 / espressif32 6.9.0; each firmware uses 21,552 static RAM bytes and 444,737 flash bytes. Optional Logging compiled/linked and smoke test ran using actual dependency headers and Logging's existing host Arduino shim. No physical hardware execution. All 63 vendor checksums and duplicate demo files verified. Added docs, changelog, planned 1.0.0 manifests, CMake and CI. Beginning publication of verified files to main only.
